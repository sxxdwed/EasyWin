<Window x:Class="EasyWin.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="EasyWin 1.3.0-beta.1 — тестовая версия"
        Width="1280"
        Height="760"
        MinWidth="1080"
        MinHeight="620"
        Background="{StaticResource WindowBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="14"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#0E1829" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="28,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock FontSize="22" FontWeight="SemiBold" Text="EasyWin" />
                    <TextBlock Margin="0,3,0,0" Foreground="{StaticResource MutedTextBrush}" Text="Безопасная локальная переустановка Windows" />
                </StackPanel>
                <Border Grid.Column="1" Padding="12,6" CornerRadius="16" Background="#24304A" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{StaticResource WarningBrush}" Margin="0,0,8,0" />
                        <TextBlock FontWeight="SemiBold" Text="{Binding ModeLabel}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="28,24,28,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="24" />
                <ColumnDefinition Width="340" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Margin="0,0,0,16" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource MutedTextBrush}" Text="ЭТАПЫ" />
                <ItemsControl ItemsSource="{Binding Stages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Width="18" Height="18" CornerRadius="9" BorderThickness="2" BorderBrush="{StaticResource BorderBrush}" Background="{Binding StatusBrush}" VerticalAlignment="Top" Margin="0,1,0,0" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                    <TextBlock Margin="0,3,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="{Binding Description}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Border Style="{StaticResource CardStyle}" Padding="14" Margin="0,8,0,0" Background="#121F34">
                    <StackPanel>
                        <TextBlock FontWeight="SemiBold" Text="Что сохранится" />
                        <TextBlock Margin="0,6,0,0" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="UEFI, Secure Boot и TPM не изменяются. EasyWin использует штатный Windows Boot Manager." />
                    </StackPanel>
                </Border>
            </StackPanel>

            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <TextBlock FontSize="28" FontWeight="SemiBold" Text="Настройка установки" />
                    <TextBlock Margin="0,6,0,22" Foreground="{StaticResource MutedTextBrush}" Text="Выберите официальный образ, редакцию и целевой диск. Проверки выполняются до любых изменений." />

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Образ Windows" />
                            <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Официальный ISO с install.wim или install.esd" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="10" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding IsoPath, Mode=OneWay}" AutomationProperties.Name="Путь к ISO" />
                                <Button Grid.Column="2" Command="{Binding BrowseIsoCommand}" Content="Выбрать ISO" AutomationProperties.Name="Выбрать ISO Windows" />
                            </Grid>
                            <Grid Margin="0,14,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Редакция" />
                                    <ComboBox ItemsSource="{Binding Editions}" SelectedItem="{Binding SelectedEdition}" DisplayMemberPath="DisplayName" IsEnabled="{Binding HasEditions}" AutomationProperties.Name="Редакция Windows" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Язык" />
                                    <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage}" AutomationProperties.Name="Язык Windows" />
                                </StackPanel>
                            </Grid>
                            <Border Margin="0,12,0,0" Padding="12,9" CornerRadius="8" Background="#172A49" BorderBrush="#31558B" BorderThickness="1">
                                <TextBlock FontWeight="SemiBold" Foreground="#AFCBFF" Text="{Binding EditionSummary}" />
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Диски" />
                            <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Куда установить Windows" />
                            <ComboBox ItemsSource="{Binding Disks}" SelectedItem="{Binding SelectedDisk}" DisplayMemberPath="DisplayName" AutomationProperties.Name="Целевой диск" />
                            <TextBlock Margin="0,10,0,6" Foreground="#FFB7BB" Text="Диск Windows БУДЕТ ОЧИЩЕН" />
                            <TextBlock Margin="0,10,0,6" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Text="Хранилище установки — отдельный диск НЕ БУДЕТ ОЧИЩЕН. Выберите диск Windows для режима с защищённым временным разделом." />
                            <ComboBox ItemsSource="{Binding StagingDisks}" SelectedItem="{Binding SelectedStagingDisk}" DisplayMemberPath="DisplayName" AutomationProperties.Name="Хранилище установки" />
                            <TextBlock Margin="0,14,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Дополнительно очистить второй диск (необязательно)" />
                            <ItemsControl ItemsSource="{Binding AdditionalEraseDisks}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,6" Padding="10,5" CornerRadius="7" Background="#17233A" BorderBrush="#33435E" BorderThickness="1">
                                            <CheckBox IsChecked="{Binding IsSelected}" AutomationProperties.Name="Очистить дополнительный диск">
                                                <TextBlock Text="{Binding Disk.DisplayName}" />
                                            </CheckBox>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Border Margin="0,8,0,0" Padding="12,9" CornerRadius="8" Background="#172A49" BorderBrush="#31558B" BorderThickness="1">
                                <TextBlock FontWeight="SemiBold" Foreground="#AFCBFF" Text="{Binding DiskSelectionSummary}" />
                            </Border>
                            <Border Margin="0,12,0,0" Padding="12" CornerRadius="8" Background="#261A24" BorderBrush="#643944" BorderThickness="1">
                                <TextBlock Foreground="#FFB7BB" Text="Целевой диск Windows очищается всегда. Отмеченный второй диск тоже будет полностью переразмечен и получит один новый раздел Data. Временный раздел EasyWin защищён до успешной загрузки новой Windows." />
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Профиль и драйверы" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Профиль" />
                                    <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="DisplayName" AutomationProperties.Name="Профиль Windows" />
                                    <TextBlock Margin="0,7,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Text="{Binding SelectedProfile.Description}" />
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Margin="0,0,0,6" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Драйверы" />
                                    <ComboBox ItemsSource="{Binding DriverModes}" SelectedItem="{Binding SelectedDriverMode}" DisplayMemberPath="DisplayName" AutomationProperties.Name="Режим драйверов" />
                                </StackPanel>
                            </Grid>
                            <Border Margin="0,14,0,0" Padding="12,9" CornerRadius="8" Background="#172A49" BorderBrush="#31558B" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold" Foreground="#AFCBFF" Text="Активация Windows" />
                                    <TextBlock Margin="0,3,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Text="Автоматически через штатную цифровую лицензию или совместимый OEM-ключ UEFI. Лицензия и ключ продукта в EasyWin не включены." />
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Программы" />
                            <TextBlock Margin="0,0,0,3" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="Обычные и фирменные программы. Установщик допускается только после проверки подписи, пути и SHA-256." />
                            <TextBlock Margin="0,0,0,12" FontSize="12" FontWeight="SemiBold" Foreground="#AFCBFF" Text="{Binding SelectedApplicationsSummary}" />
                            <ItemsControl ItemsSource="{Binding Applications}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,12,8" Padding="10,8" CornerRadius="7" Background="#17233A" BorderBrush="#33435E" BorderThickness="1" IsEnabled="{Binding IsAvailable}">
                                            <CheckBox IsChecked="{Binding IsSelected}" AutomationProperties.Name="{Binding Name}">
                                                <StackPanel>
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                                    <TextBlock Margin="0,3,0,0" FontSize="10" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" Text="{Binding Details}" />
                                                </StackPanel>
                                            </CheckBox>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Проверка готовности" />
                                <ItemsControl ItemsSource="{Binding Checks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="12" />
                                                    <ColumnDefinition Width="10" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <Ellipse Width="10" Height="10" Fill="{Binding StatusBrush}" VerticalAlignment="Center" />
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" />
                                                    <TextBlock Margin="0,2,0,0" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Text="{Binding Message}" />
                                                </StackPanel>
                                                <TextBlock Grid.Column="3" Margin="8,0,0,0" FontSize="11" FontWeight="SemiBold" Foreground="{Binding StatusBrush}" Text="{Binding StatusLabel}" VerticalAlignment="Center" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button Command="{Binding RunChecksCommand}" Content="Проверить снова" IsEnabled="{Binding IsNotBusy}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardStyle}" Visibility="{Binding HasProgress, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Ход подготовки" />
                                <TextBlock FontWeight="SemiBold" Text="{Binding CurrentProgress.Stage}" />
                                <TextBlock Margin="0,4,0,10" FontSize="12" Foreground="{StaticResource MutedTextBrush}" Text="{Binding CurrentProgress.Message}" />
                                <ProgressBar Height="8" Minimum="0" Maximum="100" Value="{Binding CurrentProgress.Percent}" Foreground="{StaticResource AccentBrush}" Background="#25334D" />
                                <TextBlock Margin="0,8,0,0" HorizontalAlignment="Right" FontSize="12" Text="{Binding CurrentProgress.PercentText}" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="1" Margin="0,16,0,0" Padding="18" CornerRadius="12" Background="#121D31" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <CheckBox IsChecked="{Binding DestructiveAcknowledged}" AutomationProperties.Name="Подтверждение удаления данных">
                            <TextBlock MaxWidth="280" Text="{Binding DestructiveAcknowledgementText}" />
                        </CheckBox>
                        <TextBlock Margin="0,8,0,14" FontSize="11" Foreground="{StaticResource MutedTextBrush}" Text="После нажатия потребуется вручную подтвердить точные Device ID всех выбранных дисков." />
                        <Button Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ReinstallCommand}" Content="{Binding PrimaryActionLabel}" IsEnabled="{Binding CanStart}" AutomationProperties.Name="Переустановить Windows" />
                        <TextBlock Margin="0,10,0,0" TextAlignment="Center" FontSize="11" Foreground="{StaticResource DangerBrush}" Text="{Binding BlockingMessage}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
